/**
 * DOM Methods
 *
 * FINDING ELEMENTS
 * document.getElementById()
 * document.querySelector()
 * document.querySelectorAll()
 * document.getElementsByTagName()
 * document.getElementsByClassName()
 *
 * CREATE CONTENT / ELEMENTS
 * document.createElement()
 * document.createTextNode()
 * document.createDocumentFragment()
 *
 * INSERTING, REMOVING or REPLACING CONTENT
 * parentElement.appendChild(childNode)
 * parentElement.append(childNode || DOMString)
 * parentElement.insertBefore(newNode, referenceNode)
 * parentNode.replaceChild(newChild, oldChild);
 * targetElement.insertAdjacentElement('beforebegin', element); //afterbegin, beforeend, afterend
 * parentElement.removeChild(childNode)
 * NOTE: when you append an element that already exists on the page, it will be moved
 *
 * STYLING
 * element.style.propName =
 * element.className =
 * element.classList.add('class')
 * element.classList.remove('class')
 * element.classList.toggle('class')
 *
 * DOM PROPERTIES
 * element.tagName =
 * element.innerHTML =
 * element.textContent =
 * element.id =
 * element.src =
 * element.alt =
 *
 * ATTRIBUTES
 * element.getAttribute('data-attr')
 * element.setAttribute('data-attr', '123')
 * element.hasAttribute('data-attr')
 *
 */
